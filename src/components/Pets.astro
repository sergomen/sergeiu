---
import WorkItem from './PetItem.astro';
import site from "../data/site.json";
const title = `My Pet`;
const {pets} = site;
---
<section id="work">
    <h3>{title}</h3>
    <div class="portfolio-grid">
        {pets.map((item) => (
            <WorkItem item={item} />
        ))}
    </div>
</section>
<style lang="scss">
.portfolio-grid {
    display: grid;
    @media (min-width: 450px) {
        grid-template-columns: 1fr 1fr;
    }
}
</style>